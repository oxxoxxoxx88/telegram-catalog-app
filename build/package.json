{
  "name": "telegram-catalog-app",
  "version": "1.0.1",
  "description": "Catalog app for Telegram bot",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "mkdir -p build && cp -r * build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "webpack": "^5.88.1",
    "webpack-cli": "^5.1.4"
  },
  "author": "Julian",
  "license": "ISC"
}